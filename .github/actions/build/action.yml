name: Preview Pull Request
description: Builds a pull request

inputs:
  folder:
    description: The folder to try to build from
    required: false
    default: ./

runs:
  using: composite
  steps:
    - run: |
      cd "${{ inputs.folder }}"
      echo "${{ secrets.TEST }}" > secret